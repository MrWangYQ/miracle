export const name = (state) => {
  return state.name;
}

export const age = (state) => {
  return state.age;
}

export const address = (state) => {
  return state.address;
}

export const boddy = (state) => {
  return state.hoddy;
}

export const specialty = (state) => {
  return state.age;
}